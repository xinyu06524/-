#include <stdio.h>
#include <stdlib.h>
int main() {
    char s[5];
    scanf("%s", &s);
    for(int i=0; i<5; i++){
        int num=s[i]-'0';
        for(int j=0; j<num; j++){
            printf("*");
        }
        printf("\n");
    }
	return 0;
}
