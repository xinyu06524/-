#include <stdio.h>
#include <stdlib.h>
int main(){
    int min;
    scanf("%d", &min);
    double cost=(double)min*0.9;
    if(800<min&&min<1500)cost*=0.9;
    else if(min>=1500)cost*=0.79;
    printf("%.1lf", cost);
	return 0;
}
