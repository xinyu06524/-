#include <stdio.h>
#include <stdlib.h>
int main(){
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    int max;
    if(a>b)max=a;
    else max=b;
    if(c>max)max=c;
    printf("%d", max);
	return 0;
}
