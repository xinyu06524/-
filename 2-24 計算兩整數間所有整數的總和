#include <stdio.h>
#include <stdlib.h>
int main(){
    int n;
    scanf("%d", &n);
    while(n--){
        int a, b;
        scanf("%d %d", &a, &b);
        int total;
        if(b>a)total=(a+b)*(b-a+1)/2;
        else total=(a+b)*(a-b+1)/2;
        printf("%d\n", total);
    }
	return 0;
}
