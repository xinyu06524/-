#include <iostream>
using namespace std;

int main(){
    int n;
    cin >> n;
    while(n--){
        int a, b;
        cin >> a >> b;
        int total;
        if(b>a) total=(a+b)*(b-a+1)/2;
        else total=(a+b)*(a-b+1)/2;
        cout << total << endl;
    }
	return 0;
}
