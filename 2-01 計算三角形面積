#include <iostream>
#include <iomanip>
using namespace std;

int main(){
    int a, b;
    cin >> a >> b;
    double area=(double)(a*b)/2;
    cout << "Triangle area:" << fixed << setprecision(1) << area;
	return 0;
}
