#include <stdio.h>
int main(){
    int n;
    scanf("%d", &n);
    while(n--){
        int m;
        scanf("%d", &m);
        if(m<=30) 
            printf("%d\n", 1<<m);    //用位移避免浮點問題
        else 
            printf("Value of more than 31\n");
    }
    return 0;
}
