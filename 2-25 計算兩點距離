#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main(){
    int x1, y1, x2, y2;
    scanf("%d %d %d %d", &x1, &y1, &x2, &y2);
    double dis=sqrt(pow((double)(x2-x1), 2)+pow((double)(y2-y1), 2));
    printf("%.2lf", dis);
	return 0;
}
