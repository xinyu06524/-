#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main(){
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;
    double dis=sqrt(pow((double)(x2-x1), 2)+pow((double)(y2-y1), 2));
    cout << fixed << setprecision(2) << dis;
	return 0;
}
