#include <stdio.h>
#include <stdlib.h>
int main(){
    int n;
    scanf("%d", &n);
    while(n--){
        int i;
        for(i=2; i<=n; i++){
            if(n%i==0)break;
        }
        if(i==n)break;
    }
    printf("%d", n);
	return 0;
}
