#include <stdio.h>
#include <stdlib.h>
int main(){
    int n;
    scanf("%d", &n);
    while(n--){
        double s;
        scanf("%lf", &s);
        double area=s*s;
        area = floor(area * 10 + 0.5) / 10;
        printf("%.1lf\n", area);
    }
	return 0;
}
