#include <iostream>
using namespace std;

int main(){
    int n;
    cin >> n;
    for(int i=0; i<8; i++){
        unsigned offset=7-i;
        cout << ((n&(1<<offset))>>offset);		//位元運算
    }
	return 0;
}
